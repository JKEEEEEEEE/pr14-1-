name: CI

on:
  push:
    branches:
      - master 
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release

    - name: Run syntax check
      run: |
        dotnet build --no-restore --configuration Release --verbosity quiet
        dotnet tool install --global StyleCop.Analyzers --version 1.2.0
        dotnet tool install --global dotnet-format --version 5.0.400
        dotnet format --check --verbosity diagnostic